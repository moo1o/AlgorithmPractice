#include <cstdio>

int M, N, X, Y;

void dfs(int cnt, int x, int y) {
	if(x<M)
}

int main(void) {
	int T;
	scanf("%d", &T);

	for (int tc = 1; tc <= T; tc++) {
		scanf("%d%d%d%d", &M, &N, &X, &Y);

	}

	return 0;
}